name: bugigangas_go_server
description: Backend REST para Bugigangas Go (conexão com Neon PostgreSQL)
publish_to: none

environment:
  sdk: ^3.12.1

dependencies:
  shelf: ^1.4.2
  shelf_router: ^1.1.4
  shelf_cors_headers: ^0.1.5
  postgres: ^3.0.0
  dotenv: ^4.2.0
